package com.dias.dias.repository;

import com.dias.dias.model.RequestValidator;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestValidatorRepository extends JpaRepository<RequestValidator, String> {
}